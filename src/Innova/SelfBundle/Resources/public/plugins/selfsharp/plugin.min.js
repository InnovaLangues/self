tinymce.PluginManager.add("selfsharp", function (n) {
    n.addButton("selfsharp", {
        text: '#...#',
        icon: false,
        onclick : function() {
            var node = tinyMCE.activeEditor.selection.getContent();
            n.insertContent("#"+ node +"#");
         }
    })
});

