            <nav class="navbar navbar-inverse navbar-static-top">
                <div class="navbar-header">
                    <ul class="nav navbar-nav">
                        <li class="active"><a style="cursor: default;" href="#">SELF</a></li>
                    </ul>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        {% if app.user is not null and is_granted('ROLE_ADMIN') %}
                            <li>
                                <a href="{{ path('admin_user') }}">
                                    <span class="glyphicon glyphicon-user"></span> {{ 'menu.users' | trans }}
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('editor_groups') }}">
                                    <span class="glyphicon glyphicon-certificate"></span> {{ 'menu.groups' | trans }}
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('export') }}">
                                    <span class="glyphicon glyphicon-export"></span>
                                    {{ 'menu.csv_export' | trans }}
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('show_tests') }}">
                                    <span class="glyphicon glyphicon-play-circle"></span>
                                    {{ 'menu.tests' | trans }}
                                </a>
                            </li>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                    <span class="glyphicon glyphicon-edit"></span>
                                    {{ 'menu.editor' | trans }} <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu">
                                    <li class="dropdown-submenu">
                                        <a tabindex="-1" href="#">Tests</a>
                                        <ul class="dropdown-menu">
                                            <li><a href="{{path('editor_tests_show')}}">{{ 'editor.test.all_tests' | trans }}</a></li>
                                            <li><a href="{{path('editor_tests_archived_show')}}">{{ 'editor.test.archived' | trans }}</a></li>
                                            <li class="divider"></li>
                                            {% set languages = get_languages() %}
                                            {% for language in languages %}
                                                <li><a href="{{path('editor_tests_by_language_show', { 'languageId': language.id })}}"> {{language.name | trans }}</a></li>
                                            {% endfor %}
                                            <li class="divider"></li>
                                            <li><a href="{{ path('editor_test_create') }}">
                                                <span class="glyphicon glyphicon-plus"></span> {{ 'editor.test.create' | trans }}
                                            </a></li>
                                            <li class="dropdown-submenu">
                                                <a tabindex="-1" href="#"><span class="glyphicon glyphicon-star"></span> Favoris</a>
                                                {% if app.user.favoritesTests|length > 0 %}
                                                <ul class="dropdown-menu" id="user-favorites">
                                                    {% for favorite in app.user.favoritesTests %}
                                                        <li data-favorite-id="{{ favorite.id }}"><a  href="{{ path('editor_test_questionnaires_show', { 'testId': favorite.id }) }}">{{ favorite.name }}</a></li>
                                                    {% endfor %}
                                                </ul>
                                                {% endif %}
                                            </li>
                                        </ul>
                                    </li>
                                    <li class="dropdown-submenu">
                                        <a tabindex="-1" href="#">{{ 'editor.task.tasks' | trans }}</a>
                                        <ul class="dropdown-menu">
                                            <li><a href="{{path('editor_questionnaires_show')}}">{{ 'editor.task.all_tasks' | trans }}</a></li>
                                            <li class="divider"></li>
                                            {% for language in languages %}
                                                <li><a href="{{path('editor_questionnaires_by_language_show', { 'languageId': language.id })}}">{{ language.name | trans }}</a></li>
                                            {% endfor %}
                                            <li class="divider"></li>
                                            <li><a href="{{path('editor_questionnaires_orphan_show')}}">{{ 'editor.task.orphan' | trans }}</a></li>
                                        </ul>
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        <li>
                            <a data-toggle="modal" href="#helpModal">
                                <span class="glyphicon glyphicon-question-sign"></span>
                                {{ 'menu.help' | trans }}
                            </a>
                        </li>
                        {# Cette page n'est pas prête pour le pilote 3. #}
                        {# <li><a data-toggle="modal" href="#creditModal">{{ 'menu.credit' | trans }}</a></li> #}
                    </ul>
                    <ul class="nav navbar-nav pull-right">
                        <li>
                            <a href="{{ path('self_user_display') }}">
                                <span class="glyphicon glyphicon-user"></span> {{ app.user.username }}
                            </a>
                        </li>
                         <li>
                            <a href="{{ path('fos_user_security_logout') }}">
                                <span class="glyphicon glyphicon-log-out"></span> {{ 'menu.logout' | trans }}
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
