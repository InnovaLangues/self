<div class="col-md-12">
    <div class="panel panel-default">
        <div class="panel-heading">La vidéo d'aide pour la compréhension écrite courte</div>
        <video controls>
            <source src="{{ asset('upload/media/') ~ 'Tuto-SELF-Indications-Visuelles-1-eec.webm' }}" poster="{{ asset('bundles/innovaself/images/') ~ "video.png"}}" >
            Your browser does not support the video tag.
        </video>
    </div>

    {% set displayHelp = 0 %}
    <a href="{{ path('test_start', { 'id': test.id,  'displayHelp': displayHelp } )}}" class="btn btn-primary">{{ 'trace.test_continue' | trans }}</a>

</div>
