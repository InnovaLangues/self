{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Test</h1>

    <table class="record_properties table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_edit', { 'id': entity.id }) }}" class="btn btn-primary">Edit</a>

    <hr>

    {{ form_start(delete_form) }}
        {{ form_errors(delete_form) }}
        <a href="{{ path('test') }}" class="btn btn-default">Back to the list</a>
        {{ form_widget(delete_form.submit, { 'attr': {'class': 'btn btn-danger'} }) }}
        {{ form_rest(delete_form) }}
    {{ form_start(delete_form) }}

{% endblock %}
