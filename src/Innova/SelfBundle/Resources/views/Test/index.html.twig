{% extends '::base.html.twig' %}


{% block body -%}
    <h1>Test list</h1>
    <table class="records_list table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Affecter</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('test_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td class="text-right">
                    <a href="{{ path('display_questionnaire', { 'testId': entity.id, 'testId': entity.id }) }}"><span class="glyphicon glyphicon-screenshot"></span></a>
                </td>
                <td class="text-right">
                    <a href="{{ path('test_edit_admin', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-edit"></span></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('test_new') }}" class="btn btn-primary">Create a new entry</a>

    {% endblock %}
