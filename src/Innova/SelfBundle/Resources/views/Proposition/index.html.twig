{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Proposition list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Audiourl</th>
                <th>Title</th>
                <th>Rightanswer</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('proposition_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.audioUrl }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.rightAnswer }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('proposition_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('proposition_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('proposition_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
