{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ 'user.edit' | trans }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_user') }}">
            {{ 'user.back_to_list' | trans }}
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
