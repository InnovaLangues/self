{% if subquestion.media is not null %}
    {% set mediaType = subquestion.media.mediaType.name %}

    <div class="col-md-{{ propWidth }} proposition-APPT-container">
        <div class="proposition-APP">
        {% if mediaType == "texte" %}
                {{ subquestion.media.description | raw }}
        {% elseif mediaType == "audio" %}
            {{ macros.audio(subquestion.media.id, subquestion.media, "audio.png", 100, 0, "") }}
        {% elseif mediaType == "image" %}
            {{ macros.imageAPP(subquestion.media.id, subquestion.media, 'player.tooltip.enlarge' | trans, "app"~question.subquestions |length ) }}
        {% endif %}
        </div>
    </div>
{% endif %}
