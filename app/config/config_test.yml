imports:
    - { resource: config_dev.yml }

parameters:
    test.client.class: Innova\SelfBundle\Library\Testing\TransactionalTestClient

doctrine:
    # ...
    dbal:
        host:     localhost
        dbname:   self_testdb
        user:     self_testdb
        password: self_testdb

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true
